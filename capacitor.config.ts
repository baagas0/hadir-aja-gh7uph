import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hadir.aja',
  appName: 'Hadir Aja',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
